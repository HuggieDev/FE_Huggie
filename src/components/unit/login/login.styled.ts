import styled from "styled-components";

export const Container = styled.div`
    overflow: hidden;
    position: relative;
`
export const Image = styled.img`
    width: 100%;
    height: 100vh;
    size: cover;
`